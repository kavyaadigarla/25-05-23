#include<stdio.h>
main()
{
    char str[100];
    scanf("%s",str);
    int i,len=0;
    while (str[len]!='\0')
    {
        len++;
    }
    for (i=len-1;i>=0;i--)
    {
        printf("%c",str[i]);
    }
    printf("\n");
    printf("%s\n",str);
}
